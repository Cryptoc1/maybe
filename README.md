# no
Like `yes`, but `no`.

### Usage
`no [expletive]`

### Compile/Install
`gcc -o no main.o; cp no /usr/local/bin`

###
I'm a stupid little shit for actually putting time into this.
You're welcome.
